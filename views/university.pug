extends layout

block content
  h1= title
  - var result =[{unvName:"NWMSU", unvSid:"12", unvFee:2000000},{unvName:"UAB", unvSid:"22", unvFee:1500000},{unvName:"UBC ", unvSid:"30", unvFee:3000000}]
  div(class="container-fluid")
  div(class="row")
   each k,index  in results 
    
     -let destination = `dispatch(\"/university/update?id=${k._id}\")`
     div(class="row-sm-2", class="m-1")  #{k.unvName}(#{k.unvSid}) is #{k.unvFee}
     button.btn( class="btn-primary ml-1", onclick= destination) Update
     script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }
        function dispatch(path){
        window.location.href=path
        }

